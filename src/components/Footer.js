import React from "react";

function Footer({id,content}) {
    return (
      <div className="main" id={id}>
      <h1>{content}</h1>
         </div>
     
    );
}

export default Footer;
